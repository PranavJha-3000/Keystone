/**
 * @keystone/shared-types — Shared TypeScript types for the Keystone voting system.
 *
 * Contains domain types (Ballot, Vote, Tally, etc.) and contract ABI types
 * shared between the frontend, backend, and indexer.
 *
 * No implementation yet — this is a scaffold.
 */

export const SHARED_TYPES_VERSION = '0.1.0';
