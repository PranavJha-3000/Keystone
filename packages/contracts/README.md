# @keystone/contracts

Solidity smart contracts for the Keystone voting system, built with Foundry. Contains the ballot box, eligibility registry, and verifier contracts.

Layout follows the standard `forge init` structure: `src/`, `test/`, `script/`.
