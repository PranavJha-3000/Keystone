{
  "name": "@keystone/circuits",
  "version": "0.1.0",
  "private": true,
  "description": "ZK circuit definitions and artifacts for Semaphore-based eligibility and vote verification",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@keystone/shared-types": "workspace:*",
    "@noble/hashes": "^1.5.0",
    "@semaphore-protocol/group": "^4.0.3",
    "@semaphore-protocol/identity": "^4.0.3",
    "@semaphore-protocol/proof": "^4.0.3",
    "@zk-kit/artifacts": "^2.0.1"
  },
  "devDependencies": {
    "@keystone/config": "workspace:*",
    "@types/node": "^20.0.0",
    "typescript": "^5.7.0",
    "eslint": "^9.17.0",
    "vitest": "^2.1.0"
  }
}
