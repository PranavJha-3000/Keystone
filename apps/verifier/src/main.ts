/**
 * @keystone/verifier — Client-side election verifier.
 *
 * This app runs entirely in the browser with no server dependency.
 * It verifies ballot inclusion, tally correctness, and proof validity
 * using only the election's public parameters and on-chain data.
 *
 * No implementation yet — this is a scaffold.
 */

import { SHARED_TYPES_VERSION } from '@keystone/shared-types';

console.log(`Keystone Verifier v0.1.0 (shared-types ${SHARED_TYPES_VERSION})`);
