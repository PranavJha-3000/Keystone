# @keystone/verifier

Static verifier app for the Keystone voting system. Runs entirely client-side — no server, no backend dependency. Open `dist/index.html` directly to verify ballot inclusion, tally correctness, and proof validity.
