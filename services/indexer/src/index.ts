/**
 * @keystone/indexer — On-chain event indexer for the Keystone voting system.
 *
 * Subscribes to BallotBox contract events and writes them to a database
 * so the API can serve fast queries without hitting the RPC node.
 *
 * No implementation yet — this is a scaffold.
 */

console.log('Indexer scaffold — implementation pending');
